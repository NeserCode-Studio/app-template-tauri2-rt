# @super 命令系统 - 完成

## 任务目标
实现 `@super` 前缀触发的隐藏命令系统，抽象化接口便于后续扩展。

## 修改的文件

### 前端

**src/components/framework/CommandDialog.tsx** - `@super` 过滤逻辑
- 修复 `useDebouncedCallback` → `useDebounceFn`（ahooks v3 API）
- 修复 `setLang`/`setThemeMode` 从 undefined context 取值导致的 TS 错误

**src/composables/useSuperCommand.ts** - 命令注册与管理
- `SuperCommandItem` 接口：`id`, `keywords`, `invoke?`, `invokeArgs?`, `action?`, `confirm?`
- `createBuiltinCommands()` 工厂：navigate × 4, lang × 2, theme × 2, devtools × 1
- `useSuperCommand()` hook：提供 `commands`, `register`, `search`, `execute`

**src/lib/i18n.resource.ts** - 新增 10 个翻译 key（en-US + zh-CN）
- `SuperCommand.navigate.home` ~ `SuperCommand.devtools.open`

### 后端

**src-tauri/src/lib.rs** - `open_devtools` 命令
```rust
use tauri::Manager;

#[tauri::command]
fn open_devtools(app: tauri::AppHandle) -> Result<(), String> {
    app.get_webview_window("main")
        .ok_or_else(|| "Main window not found".to_string())?
        .open_devtools();
    Ok(())
}
```

**src-tauri/Cargo.toml** - 新增 `tauri/devtools` feature
```toml
tauri = { version = "2", features = ["tray-icon", "devtools"] }
```

## 初始命令清单

| ID | 触发词 | 类型 |
|---|---|---|
| navigate.home | 首页/home/主页 | action |
| navigate.settings | 设置/settings/配置 | action |
| navigate.software | 软件/software | action |
| navigate.framework | 框架/framework | action |
| lang.en-US | english/英文/en | action |
| lang.zh-CN | 中文/chinese/zh | action |
| theme.light | light/亮色/浅色 | action |
| theme.dark | dark/暗黑/深色 | action |
| devtools.open | devtools/开发者工具 | invoke |

## 使用方式
1. 打开命令面板：`Ctrl+K`
2. 输入 `@super ` 触发超级命令模式
3. 输入关键词搜索并回车执行

## 验证
- TypeScript: `npx tsc --noEmit` ✅
- Rust: `cargo check` ✅